# Code Expert MCP - Environment Variables
# Copy this file to .env and fill in your values

# ============================================================================
# Repository Management API Configuration
# ============================================================================

# API Password for repository management endpoints (REQUIRED for API access)
# This password is required to use the REST API endpoints:
# - POST /api/repos/clone - Clone a repository to cache
# - GET /api/repos - List all cached repositories
# - DELETE /api/repos - Delete a cached repository
REPO_API_PASSWORD=your-secure-password-here

# ============================================================================
# Repository Cache Configuration
# ============================================================================

# Maximum number of repositories to cache (default: 50)
MAX_CACHED_REPOS=50

# Cache directory path (default: /cache in Docker, .cache locally)
CACHE_DIR=/cache

# ============================================================================
# Git Provider Authentication (Optional)
# ============================================================================

# GitHub Personal Access Token (optional, but recommended for higher rate limits)
# Create at: https://github.com/settings/tokens
# Required scopes: repo (for private repos), or public_repo (for public only)
GITHUB_TOKEN=

# Azure DevOps Personal Access Token (optional)
# Create at: https://dev.azure.com/[your-org]/_usersSettings/tokens
# Required scopes: Code (Read)
AZURE_DEVOPS_TOKEN=

# ============================================================================
# Webhook Configuration (Optional - for auto-refresh feature)
# ============================================================================

# Webhook secret for GitHub/Azure DevOps webhook authentication
# This is SEPARATE from REPO_API_PASSWORD
WEBHOOK_SECRET=
